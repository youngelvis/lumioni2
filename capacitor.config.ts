import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.lumioni',
  appName: 'lumioni',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
